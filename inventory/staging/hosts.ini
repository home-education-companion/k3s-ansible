[master]
10.10.0.12
10.10.0.10
10.10.0.11

# all masters also run workloads; kept empty so k3s_cluster:children parses
[node]




# only required if proxmox_lxc_configure: true
# must contain all proxmox instances that have a master or worker node
# [proxmox]
# 192.168.30.43

[k3s_cluster:children]
master
node

